default:
  suites:
    domain:
      contexts:
        - DomainContext

    services:
      contexts:
        - ServiceContext:
            courseEnrolments: "@cjm.training.enrolment.course_enrolments"

  extensions:
    Behat\Symfony2Extension:
      kernel:
        bootstrap: ~
        class: App\Kernel

    Behat\MinkExtension:
      sessions:
        symfony:
          symfony2: ~
